<html lang="en">
  <head>
    <meta charset="utf-8" />
    <title>My first view</title>
    <link rel="stylesheet" href="stylesheets/style.css" />
  </head>
  <body>
    {{!-- <h1>Hello {{name}}!</h1>
    {{#if lastName}}
      <h2> last name exist and it is : {{lastName}} </h2>
    {{else}}
      <p> please provide the lastname </p>
    {{/if}} --}}
    {{!-- <h1>Hello {{name}} {{lastName}}!</h1>
    {{#unless address}}
      <h3>WARNING: We can't find the address!</h3>
    {{/unless}} --}}
    {{!-- {{#each cities}}
      <p> {{@index}} {{this}} </p>
    {{/each}}
    {{#each address}}
      <p> {{@key}}: {{this}} </p>
    {{/each}} --}}
    {{!-- {{#each cities}}
      {{#if @first}}
        <li><b>{{this}}</b></li>
      {{else if @last}}
        <li><i>{{this}}</i></li>
      {{else}}
        <li>{{this}}</li>
      {{/if}}
    {{/each}} --}}
    {{#with address}}
      {{street}}
      {{number}}
    {{/with}}
    {{!-- {{address.street}}
    {{address.number}} --}}
  </body>
</html>